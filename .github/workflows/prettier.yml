name: Prettier code formatter

on:
  # Disabled - not needed for this site
  workflow_dispatch:

jobs:
  check:
    # This workflow is disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Prettier workflow is disabled"
